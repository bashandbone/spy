interface:
  display_name: "Spy"
  short_description: "Repo-specific patterns and workflows for spy"
  default_prompt: "Use the spy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true